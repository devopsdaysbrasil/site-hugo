sudo: required

services:
  - docker

script:
  - docker container run --rm -it -v $(pwd):/srv jojomi/hugo hugo

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  repo: devopsdaysbrasil/devopsdaysbrasil.github.io
  local_dir: public
  on:
    branch: master
